# Port the server listens on (used for local `npm start`; when deployed
# via the systemd socket unit, the port here is what the install script
# bakes into that unit's ListenStream — see deploy/README.md).
PORT=8097
